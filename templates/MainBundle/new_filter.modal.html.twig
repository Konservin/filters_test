<!-- Bootstrap Modal -->
<div class="modal fade" id="filterModal" tabindex="-1" aria-labelledby="filterModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header d-flex align-items-center">
                <h5 class="modal-title" id="filterModalLabel">Create New Filter</h5>
                <div class="ms-auto d-flex align-items-center">
                    <a href="{{ path('new_filter') }}" class="btn btn-primary me-2" title="Change to Non-modal">Change to Non-modal</a>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
            </div>
            <div class="modal-body d-flex flex-column">
                <!-- The form will be loaded dynamically here -->
                <div id="filterFormContainer" class="flex-grow-1 overflow-auto"></div>
            </div>
            <div class="modal-footer">
                <div class="d-flex gap-2 mt-4">
                    <button type="button" id="add-criteria" class="btn btn-secondary">Add Criteria</button>
                    <button id="save-filter-btn" type="submit" class="btn btn-primary">Save Filter</button>
                </div>
            </div>
        </div>
    </div>
</div>


